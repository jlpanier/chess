<ContentPage
    x:Name="RootPage"
    x:Class="Chess.Pages.MainPage"
    x:DataType="viewModels:MainViewModel"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converter="clr-namespace:Chess.Converter"
    xmlns:viewModels="clr-namespace:Chess.ViewModels">

    <ContentPage.Resources>
        <converter:SquareColorConverter x:Key="SquareColorConverter"/>
        <converter:SquareHighlightConverter x:Key="SquareHighlightConverter"/>
    </ContentPage.Resources>
    
    <ContentPage.BindingContext>
        <viewModels:MainViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="*,Auto">
        <Image Source="background.jpg" Aspect="AspectFill" />
        
        <Border Stroke="Brown"
            StrokeThickness="6"
            StrokeShape="Rectangle"
            Background="#D8C3A5"
            Padding="10"
            Margin="10">

            <Border.Shadow>
                <Shadow Brush="Black"
                    Opacity="0.4"
                    Offset="10,10"
                    Radius="20" />
            </Border.Shadow>

            <Grid RowDefinitions="60,*,60">

                <Label Text="{Binding BlackTakenPieces}"
                    Grid.Row="0"
                    FontFamily="ChessMerida"
                    HorizontalOptions="Start"
                    VerticalOptions="Start"
                    Margin="0,0,0,10"
                    FontSize="24"
                    TextColor="Black"/>


                <Border 
                    Grid.Row="1"
                    Stroke="White"
                    StrokeThickness="4"
                    StrokeShape="Rectangle"
                    Background="#D8C3A5"
                    Padding="4">

                    <!-- Plateau 8×8 -->
                    <Grid RowSpacing="0" ColumnSpacing="0"
                      BindableLayout.ItemsSource="{Binding Squares}">

                        <!-- 8 colonnes -->
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="40" />
                        </Grid.ColumnDefinitions>

                        <!-- 8 lignes -->
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                        </Grid.RowDefinitions>

                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="viewModels:SquareViewModel">

                                <Grid 
                                    Grid.Row="{Binding Row}" 
                                    Grid.Column="{Binding Column}" 
                                    Margin="0,0,0,0"
                                    Padding="0,0,0,0">

                                    <!-- Case -->
                                    <Border 
                                        Background="{Binding Index, Converter={StaticResource SquareColorConverter}}"
                                        StrokeShape="Rectangle"
                                        StrokeThickness="0"
                                        Margin="0,0,0,0"
                                        Padding="0,0,0,0">

                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding SelectSquareCommand}" />
                                            <TapGestureRecognizer 
                                            NumberOfTapsRequired="2"
                                            Command="{Binding DoubleClickSquareCommand}" />

                                        </Border.GestureRecognizers>

                                        <AbsoluteLayout                                    
                                            Margin="0,0,0,0"
                                            Padding="0,0,0,0">


                                            
                                            <!-- Pièce -->
                                            <Label 
                                               Text="{Binding PieceSymbol}"
                                               FontFamily="ChessMerida"
                                               FontSize="36"
                                               TextColor="{Binding PieceColor}"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center"
                                               HorizontalTextAlignment="Center"
                                               AbsoluteLayout.LayoutBounds="0.5,0.5,AutoSize,AutoSize"
                                               AbsoluteLayout.LayoutFlags="PositionProportional"/>

                                            <!-- Rond plein au centre -->
                                            <BoxView 
                                                Color="{Binding MoveColor}"   
                                                BackgroundColor="Transparent"
                                                WidthRequest="12"
                                                HeightRequest="12"
                                                CornerRadius="10"
                                                AbsoluteLayout.LayoutBounds="0.5,0.5,20,20"
                                                AbsoluteLayout.LayoutFlags="PositionProportional"
                                                IsVisible="{Binding IsAuthorizedMove
                                                }"/>

                                            <Border 
                                                Stroke="{Binding BorderMoveColor}"
                                                StrokeThickness="2"
                                                StrokeShape="Rectangle"
                                                Background="Transparent"
                                                IsVisible="{Binding ShowBorder}"
                                                AbsoluteLayout.LayoutBounds="0,0,1,1"
                                                AbsoluteLayout.LayoutFlags="All"
                                                InputTransparent="True"
                                                />
                                            

                                            <!-- Coordonnée colonne (a–h) -->
                                            <Label Text="{Binding LetterColumn}"
                                               FontSize="10"
                                               TextColor="#D4AF37"
                                               Margin="1,0,0,0"
                                               HorizontalOptions="Start"
                                               VerticalOptions="Start"
                                               IsVisible="{Binding IsFirstRow}" />

                                            <!-- Coordonnée ligne (1–8) -->
                                            <Label Text="{Binding LetterRow}"
                                               FontSize="10"
                                               TextColor="#D4AF37"
                                               Margin="1,0,0,0"
                                               HorizontalOptions="End"
                                               VerticalOptions="End"
                                               IsVisible="{Binding IsFirstColumn}" />


                                            
                                        </AbsoluteLayout>
                                    </Border>

                                    <!-- Highlight -->
                                    <Border Background="{Binding IsSelected, Converter={StaticResource SquareHighlightConverter}}"
                                        InputTransparent="True" />

                                </Grid>

                            </DataTemplate>
                        </BindableLayout.ItemTemplate>

                    </Grid>
                </Border>

                <Label Text="{Binding WhiteTakenPieces}"
                    Grid.Row="2"
                    FontFamily="ChessMerida"
                    FontSize="24"
                    HorizontalOptions="Start"
                    VerticalOptions="Start"
                    Margin="0,0,0,10"
                    TextColor="White"/>
            </Grid>
        </Border>

        <Button
            Text="&#x1F4BE;"
            Grid.Row="2"
            FontSize="20"
            BackgroundColor="DodgerBlue"
            CornerRadius="25"
            WidthRequest="40"
            HeightRequest="40"
            Command="{Binding DownloadCommand}"
            HorizontalOptions="Start"
            VerticalOptions="End"
            Shadow="{Shadow Brush=Black, Opacity=0.4, Radius=10}"
            Margin="10,10,10,10"
            Padding="5,5,5,10"
            />
        
        <Button 
            Text="↺"
            Grid.Row="2"
            HorizontalOptions="End"
            VerticalOptions="Center"
            Margin="5,5,5,5"
            Command="{Binding ResetGameCommand}"
            BackgroundColor="#B22222"
            Shadow="{Shadow Brush=Black, Opacity=0.4, Radius=10}"
            TextColor="White"
            WidthRequest="40"
            HeightRequest="40"
            CornerRadius="25"
            FontSize="32"
            Padding="0,-5,0,0"/>



    </Grid>
</ContentPage>
